amqp-rpc-server {
    amqp {
        host = "localhost"
        port = 5672
        user = "guest"
        password = "guest"
    }

    rpcQueue = "test"
}

simulation {
    amqp {
        host = "localhost"
        port = 5672
        user = "guest"
        password = "guest"
        exchange = "test.exchange"
        routing-key = "*"
        queue = "test"
    }
}
